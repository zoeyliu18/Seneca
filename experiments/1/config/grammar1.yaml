# seneca.yaml

## Where the samples will be written
save_data: experiments/grammar/baseline
## Where the vocab(s) will be written
src_vocab: experiments/grammar/baseline/grammar_vocab_src_1
tgt_vocab: experiments/grammar/baseline/grammar_vocab_tgt_1
# Prevent overwriting existing files in the folder
overwrite: False

# Corpus opts:
data:
    seneca:
        path_src: experiments/grammar/baseline/grammar_train_src_1
        path_tgt: experiments/grammar/baseline/grammar_train_tgt_1
    valid:
        path_src: experiments/grammar/baseline/grammar_dev_src_1
        path_tgt: experiments/grammar/baseline/grammar_dev_tgt_1
...
